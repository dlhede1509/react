function solution() {
	let integers = [8, 1, 6, 7, 0, 11, 0, 2, 1, 5];
	let T = integers
	let i = 0;
	let capital = T[i] ;
	
 for (const [i, integer] of integers.entries()) {
			if (i === T[i]) {
					let capital = i;
			} else {
					if (i != T[i]) {
							i++
					}
			}
			
	}
}